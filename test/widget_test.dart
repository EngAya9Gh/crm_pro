// This is a basic Flutter widget test.

import 'package:flutter_test/flutter_test.dart';
import 'package:crm_wakeel/main.dart' as app;

void main() {
  test('Main file can be imported', () {
    // This test verifies that the main.dart file can be imported without errors
    expect(app.main, isNotNull);
  });
}
